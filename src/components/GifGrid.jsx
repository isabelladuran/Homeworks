import { useState } from "react"

const GifGrid = ({category}) => {
    return(
        <>
            <div>
                <h2>{category}</h2>
            </div>
        </>
    )
}

export default GifGrid